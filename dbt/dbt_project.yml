name: security_master_ledger
version: 1.0.0
config-version: 2

profile: security_master_ledger

model-paths: ["models"]
test-paths: ["tests"]

models:
  security_master_ledger:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
